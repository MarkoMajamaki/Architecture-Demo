name: frontend
description: Client for Architecture-Demo

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.0
  flutkit: ^0.0.2
  get_it: ^6.0.0
  provider: ^5.0.0
  http: ^0.13.0
  uuid: 3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
  - lang/en.json
  - lang/fi.json